/**
 *
 * export your action creators here. for example
 *
 * export function setPropertyOnState(val) {
 *   return {
 *     type: 'SET_PROPERTY_ON_STATE'
 *     val
 *   }
 * }
 *
 *
 * or an async function might look like this 
 * (assuming you're using thunk https://github.com/gaearon/redux-thunk)
 *
 * export function fetchSomeData() {
 *   return dispatch => {
 *     Api.fecthSomeData().then(data => dispatch(setPropertyOnState(data)))
 *   }
 * }
 *
 */




